# Error Detection and Fault Handling

## Requirements
- Implement system diagnostics to detect and alert when a sensor or actuator fails (e.g., if a sensor stops providing data for more than 30 seconds) 